import { Typography } from "@mui/material"

export const Operators = () => {
    return <div style={{ marginTop: '20%' }}>
        <center>
            <Typography variant="h5" color='Background'>Operators</Typography>
        </center>
    </div>
}