import { Typography } from "@mui/material"

export const Conditionals = () => {
     return <div style={{marginTop:'20%'}}>
     <center>
     <Typography variant="h5" color='Background'>Conditional</Typography>
     </center>
 </div>
}